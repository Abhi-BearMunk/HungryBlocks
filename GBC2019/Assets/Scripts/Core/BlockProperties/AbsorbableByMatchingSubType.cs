using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbsorbableByMatchingSubType : MonoBehaviour // IBlockeProperty to prevent moving if absorbable but we are SURE that it is not the one doing the absorbing
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
