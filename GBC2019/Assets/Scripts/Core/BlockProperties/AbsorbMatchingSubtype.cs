using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class AbsorbMatchingSubtype : MonoBehaviour //, IPreTransformCellProperty to detect blocks, IPreTransformBlockProperty tomove these blocks in the same direction, IPostTransformBlockProperty to add these blocks
{
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
